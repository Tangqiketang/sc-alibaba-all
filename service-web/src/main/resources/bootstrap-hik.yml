server:
  port: 8900

spring:
  cloud:
    nacos:
      config:
        server-addr: 10.0.40.65:8848  #nacos地址
        namespace: cdf5059c-106c-40b0-9af3-75f0ded01d71
        refresh-enabled: true
        group: DEFAULT_GROUP
        file-extension: yml
        shared-configs[0]:
          data-id: common.yml # 共享配置
          group: DEFAULT_GROUP
          refresh: true
      #暴露的地址
      discovery:
        namespace: ${spring.cloud.nacos.config.namespace}
        server-addr: ${spring.cloud.nacos.config.server-addr}
        ip: 10.1.91.127  #注册的ip地址
    sentinel:
      transport:
        port: 18765
        dashboard: 10.1.91.127:8748
  datasource:
    url: jdbc:mysql://10.0.40.61:3306/service-web?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root@123
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      minimum-idle: 5
      maximum-pool-size: 10
      idle-timeout: 180000
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1
  jackson:
    default-property-inclusion: NON_NULL
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
  servlet:
    multipart:
      enabled: true
      #file-size-threshold: 2KB
      max-file-size: 104857600
      max-request-size: 104857600

############# mybatis-plus      ##############
mybatis-plus:
  executorType: reuse
  mapper-locations: classpath*:com/wm/web/mapper/xml/*.xml
  global-config:
    banner: false
    db-config:
      table-underline: true
  configuration:
    call-setters-on-nulls: true
    # 这个配置会将执行的sql打印出来，在开发或测试的时候可以用
    #log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
    map-underscore-to-camel-case: true
    cache-enabled: false

####### 日志级别 #######
logging:
  file:
    path: /opt/${spring.application.name}/logs/
  level:
    root: info
    com:
      wm:
        web: debug
##### swagger ###################
springfox:
  documentation:
    swagger-ui:
      enabled: true # false关闭swagger-ui界面 但不关闭openapi



