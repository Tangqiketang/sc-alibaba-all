version: '3'
services:
    nexus:
      image: 'sonatype/nexus3'
      container_name: "nexus"
      restart: always
      environment:
        TZ: Asia/Shanghai
      ports:
        - '8931:8081'
      volumes:
        - './data:/nexus-data'
